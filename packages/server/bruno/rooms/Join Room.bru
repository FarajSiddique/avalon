meta {
  name: Join Room
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/rooms/{{roomCode}}/join
  body: json
  auth: inherit
}

body:json {
  {
    "playerName": "NotTacoBurrito"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
